package server.connection.service;

public enum RequestType {
    FOREIGN_BANK,
    REGISTER,
    LOGIN,
    REMOVE_USER,
    CREATE_ACC,
    REMOVE_ACC,
    ADD_FUNDS,
    REMOVE_FUNDS,
    CHECK_BALANCE
}
